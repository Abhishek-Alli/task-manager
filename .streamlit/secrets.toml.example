# Database Configuration for Streamlit Cloud
# Copy this file to .streamlit/secrets.toml and fill in your database credentials
# For Streamlit Cloud, add these secrets in: Manage app > Settings > Secrets

# Option 1: Connection String (Recommended for Vercel Prisma/Postgres)
# Get this from Vercel Dashboard > Storage > Your Database > Connection String
[postgres]
connection_string = "postgresql://user:password@host:port/database?sslmode=require"

# OR Option 2: Individual Parameters
# [postgres]
# host = "your-database-host.com"
# database = "hrms_db"
# user = "postgres"
# password = "your-database-password"
# port = 5432

